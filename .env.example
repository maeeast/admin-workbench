# Runtime (pooled in prod, direct locally is fine)
DATABASE_URL="postgresql://USER:PASSWORD@HOST:PORT/DB?sslmode=require"

# Migrations/CLI (direct/non-pooled in prod)
DATABASE_URL_MIGRATIONS="postgresql://USER:PASSWORD@HOST:PORT/DB?sslmode=require"

SESSION_PASSWORD="replace-with-32+char-secret"